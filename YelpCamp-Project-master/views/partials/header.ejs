<!DOCTYPE html>
   <html lang="en">
   <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">

      <!-- Bootstrap CSS -->
      <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
      <!-- Font Awesome -->
      <script defer src="https://use.fontawesome.com/releases/v5.13.0/js/all.js"></script>
      <!-- Modernizer -->
      <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>

      <!-- CSS Stylesheet -->
      <link rel="stylesheet" href="/stylesheets/main.css">

      <title>YelpCamp</title>
   </head>
   <body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark pt-0 mb-3">
         <a class="navbar-brand" href="/"><strong> YelpCamp </strong></a>
         <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
           <span class="navbar-toggler-icon"></span>
         </button>
         <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="nav navbar-nav ml-auto">
              <% if(!currentUser) {%>
               <li class="nav-item active">
                  <a class="nav-link" href="/login" style="font-size: 0.8rem;"><i class="fas fa-user-plus"></i> Log In </a>
                </li>
                <li class="nav-item active">
                  <a class="nav-link" href="/register" style="font-size: 0.8rem;"><i class="fas fa-user"></i> Sign Up </a>
                </li>
              <% } else { %>
               <li class="nav-item">
                  <a class="nav-link" href="#" style="font-size: 0.8rem;">Current User: <em><strong><%= currentUser.username %></strong></em></a>
               </li>
               <li class="nav-item active">
                  <a class="nav-link" href="/logout" style="font-size: 0.8rem;"><i class="fas fa-user-minus"></i> Log Out </a>
               </li>
              <% } %>
             </ul>
         </div>
       </nav>
       <div class="container">
         <% if( error && error.length > 0 ) { %>
            <div class="alert alert-danger" role="alert">
               <em><%= error %></em>
            </div>
         <% } %>
         <% if( success && success.length > 0 ) { %>
            <div class="alert alert-success" role="alert">
               <em><%= success %></em>
            </div>
         <% } %>
       </div>
       